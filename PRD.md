# 企业 AI 协作工作台 PRD

版本：2026-09-19｜状态：本地验证版｜范围：制度问答、合同条款核对与人工复核交接

本文是范围与验收的主文档。系统提示词以[运行文件](app/prompts/policy_answer.md)为准；依赖与执行状态见[计划](specs/001-saas-productization/plan.md)和[任务](specs/001-saas-productization/tasks.md)。

## 1. 背景与目标

产品不重建人事、财务或 ERP 系统，而是在现有流程旁提供可核验的知识辅助。重点增强场景为工程采购与合同商务协作。当前制度样本包含物流供应商规则，不能据此宣称工程行业适配已完成。

| 用户 | 任务与痛点假设 | 交付结果 | 尚需验证 |
|---|---|---|---|
| 员工、业务经办人 | 办事前反复查制度、询问责任部门 | 答案、适用条件、原文位置或依据不足 | 高频问题及实际解决率 |
| 商务、采购经办人 | 从长合同中核对付款、结算、变更签证、质保和履约约定 | 核对清单、备注、交接记录 | 初筛耗时、遗漏、误报 |
| 法务、业务负责人 | 核实待确认项，防止依据和适用场景混淆 | 原文、待补资料及人工确认状态 | 复核流程与采纳情况 |
| 知识维护人员 | 区分知识缺口、解析、检索和生成错误 | 解析状态、回归结果及反馈摘要 | 更新工时、坏例关闭周期 |

以上是产品假设，不是已完成的用户调研。目前没有真实用户基线、客户结果或收入数据。

### 1.1 结果目标

| 目标 | 测量方法 | 当前状态 |
|---|---|---|
| 减少查找与核验耗时 | 同组任务比较原流程与辅助流程的耗时中位数，计入人工复核 | 待业务基线 |
| 帮助解决制度问题 | 已确认解决数 / 已取得结果反馈的有效问题数；另列反馈覆盖率 | 待试点；点赞率不替代解决率 |
| 降低合同初筛遗漏 | 金标准中应提示但未提示的条款数 / 应提示条款数；同时记录误报率 | 有合成回归，缺独立合同集 |
| 控制单任务成本 | 模型、运行、人工复核和知识维护成本 / 已解决任务数 | 未接入真实用量与成本归集 |
| 保持答案可核验 | 分别检查事实完整性、限定语、来源和拒答 | 可离线逐题验收 |

不套用未经测量的承接率、秒级响应或节省工时。试点先采集基线，再由业务负责人确认改善目标、观察周期和停止条件。

### 1.2 四阶段路线

| 阶段 | 范围 | 状态或门槛 |
|---|---|---|
| 一：制度问答闭环 | 解析、检索、门控、生成、引用、拒答、反馈收集和回归 | 已实现；反馈归因及案例收录仍是人工流程 |
| 二：合同商务协作增强 | 独立合同库、12 类核对、原文位置、人工备注和导出 | 本地能力已实现；尚无真实协作效果 |
| 三：受控试点与交付 | 授权语料、安全接入、真实任务验证、必要的更新机制和连接器 | 规划；敏感资料或多人访问前先完成安全验收 |
| 四：可复制服务 | 第二次交付证明共性后再评估多租户、API、配额和用量 | 条件性规划，不预设必须 SaaS 化 |

## 2. 能力边界

| 范围 | 可以做 | 不能做或兜底 |
|---|---|---|
| 制度问答 | 对已入库文本检索，生成带来源回答及有限追问 | 不保证任意问法正确；目录、相似动作及跨流程内容不能替代依据 |
| 合同核对 | 规则定位主体、金额、期限、付款结算、账户、保险、履约担保、终止续约、变更签证、质保、验收、违约 | 不是大模型自主审查；未定位不等于合同没有约定，已定位不等于审核通过 |
| 人工复核 | 待复核、已确认、需补充三种状态，备注及 Markdown 导出 | 只在会话内保存，没有服务端签名、多人协同、审批留痕或电子签署 |
| 本地访问范围 | 通过预置角色验证合同列表与检索前范围过滤 | 参数可伪造，不是真实认证；仅限可信本机环境 |
| 反馈运营 | 保存匿名赞踩、原因、问题与回答，展示汇总 | 没有工单指派、状态流转或自动闭环；不能宣称线上解决率 |
| 工作台页面 | 场景入口、快捷问题、全局助手；资讯独立运行 | 人事、财务、ERP 未连接真实业务系统 |

非目标：自主审批、付款、签约、修改交易数据、法律结论、通用办公套件，以及未经验证的多智能体或平台化架构。

## 3. 行为定义

| 场景 | 预期行为 | 当前验证或缺口 |
|---|---|---|
| 有充分正文 | 直接回答所问条件，返回文档、章节及原文 | 逐题核验，不主动扩展相邻流程 |
| 复合问题 | 分别回答时限、金额、责任主体等子问题 | 少答一个关键事实即失败，不以任一关键词命中算通过 |
| 相似动作、不同对象或阶段 | 不把车辆条款套给办公设备，不把供应商准入套给合同起草 | 已补部分对象和阶段约束，非通用语义证明 |
| 只找到目录或标题 | 不将其视为办事依据，不据标题补流程 | 目录排除和门控测试 |
| 范围外问题 | 明确依据不足，不编造部门、联系方式和办理结果 | 建议核实；没有自动人工转接单 |
| 条件缺失或制度冲突 | 目标行为是指出缺失或冲突并请求确认 | 自动版本冲突识别未实现，真实试点前人工治理 |
| 年假追问只提供入职时长 | 请求入职日期、拟休假日期与累计工龄 | 两个月可能跨日历年，不能直接推断不符合条件 |
| 合同空白、缺失或含糊 | 分别显示未定位、信息不完整、待确认及原文 | 人工回看完整合同，不能据自动状态批准签约 |
| 无授权或不存在合同 | 统一返回合同不可用，不泄露标题、金额或片段 | 仅本地角色范围验证，真实身份待阶段三 |
| 身份、合同或文本版本变化 | 不复用上一范围的审阅结果；切换身份清除复核控件 | 前端状态与接口回归 |
| 请求执行审批、付款 | 不执行；能力限于查询、核对及人工交接 | 模型没有业务写工具 |

## 4. Prompt 规格

### 4.1 单一事实来源

- 版本：`policy-answer-v2`，随问答调试字段返回。
- 系统提示词全文：[app/prompts/policy_answer.md](app/prompts/policy_answer.md)。运行时直接读取，不维护与代码脱节的副本。
- 组装逻辑：`app/agent.py` 的 `_compose_llm_answer`。
- 合同核对不使用生成式 Prompt，字段规则在 `app/contracts.py`。

### 4.2 输入、输出与约束

输入包含当前问题、最多 8 条历史消息、通过门控的同一制度片段，以及可选的来源内事实清单。事实清单来自规则抽取，不是独立人工审核，不能补充原文没有的事实。

| 分支 | 用户输入模板 | 条件 |
|---|---|---|
| 限定事实范围 | 当前问题 + 抽取事实 + 禁止扩展条件；必要时强调保留原则性限定语 | 命中语料适配规则 |
| 通用证据 | 当前问题 + 可选事实清单 + 文档、章节、原文 | 未命中特定规则 |
| 简洁或详细 | 默认直接简答；明确要求明细、全部、完整流程时可分点 | 增加篇幅不能增加未询问的业务事项 |

必须保留金额、工作日、限定语及责任主体，覆盖每个子问题，不混淆相邻流程。用户消息、文档内的指令均视为数据；不得声称已提交、审批、付款或通知他人。

表述验收示例：“原则上不超过 X”不能改成绝对上限；“人力发起申请”和“IT 开通账号”不能交换；没有咖啡机条款时不能从车辆报销规定推导结论。提示词中包含相应正反例，但提示词本身不等于质量保证。

### 4.3 变更与发布

修改 Prompt、模型、切块、检索或适配规则后，记录版本，重跑离线回归和真实模型人工审计。Embedding 或解析策略变化必须重建索引。Ollama 当前生成温度为 0，兼容接口为 0.1；低温不保证事实稳定。

未实现独立 Prompt 平台、在线灰度、自动模型优选、token 成本看板或完整提示注入防护验收。

## 5. 知识与数据

| 数据 | 位置与来源状态 | 维护责任和规则 |
|---|---|---|
| 三份制度 PDF | `data/raw/`；维护者参考公开企业资料融合整理的验证语料，不列具体企业出处 | 维护版本与验收范围；不代表特定企业现行制度，不能从文件名推断生效日期 |
| 工程采购合同合成样本 | `data/samples/`；项目维护，不对应交易 | 产品与测试维护边界用例，不替代独立真实合同集 |
| 本地合同原件 | `data/contracts/`，不进入 Git 或镜像 | 授权持有人控制上传、保留和删除；无自动到期清理 |
| 反馈与权限配置 | `data/processed/`，不进入 Git 或镜像 | 运营人工归因；匿名反馈仍可能含敏感正文 |
| 回归与人工验收题 | `data/evaluation/`、`data/answer_quality/`、`data/acceptance/` | 核验必答事实、禁止事实和出处后入集 |

资料说明见 [data/README.md](data/README.md)。驳船、拖车规则不能直接当作工程分包准入规则。

当前更新流程：人工提供文件 → 解析与切块 → 指纹校验或重建 → 评测 → 核对答案与引用。纯目录不入检索；扫描件无文本则标记失败。没有 OCR、增量同步、自动发布、制度优先级或完整发布回滚。

试点必须为每份资料指定维护角色。制度变化时提交修订依据，经责任部门确认后发布；按约定周期复盘反馈。具体人选和频率由试点负责人确认，不把计划写成已运行流程。

## 6. 工具与接口

当前是确定性检索工作流加模型生成，不是自主调用业务工具的智能体。LangGraph 连接路由、检索、回答；不可用时顺序执行同一流程。

| 接口或操作 | 输入限制 | 读写性质 | 确认及边界 |
|---|---|---|---|
| `POST /chat` | 问题 ≤1000 字符；历史 ≤8 条 | 查询和模型调用 | 门控后生成，无业务写操作 |
| 文档列表、`GET /health` | 当前实例 | 读元数据和状态 | 仅可信本机使用 |
| `POST /knowledge-base/rebuild` | 当前制度目录 | 写派生索引 | 人员主动触发，不代表企业制度发布审批 |
| 反馈提交及汇总 | 问题、回答、赞踩、原因 | 写反馈、读汇总 | 用户明确点击，不自动改知识或训练 |
| `POST /evaluation/run` | 固定题集 | 读取语料并评测 | 人员触发，不是生成质量评分 |
| `POST /contracts` | PDF、DOCX、Markdown、TXT；界面单份 ≤18 MB | 写合同与允许角色 | 法务或领导主动上传；拒绝同名覆盖 |
| 合同列表与审阅 | 角色、合同 ID、可选查询 | 读授权范围合同 | 先检查范围再检索，权限配置损坏则关闭访问 |
| 下载核对记录 | 文本哈希、条款、人工状态和备注 | 本机下载 | 操作者点击，不是自动批准或服务端归档 |

角色参数不是认证。多人、远程或敏感数据场景必须先完成服务端身份、资源权限、审计和部署评审，不能等试点结束再补。

## 7. 异常与降级

| 异常 | 当前行为 | 限制 |
|---|---|---|
| 查询向量服务失败 | 保留 BM25，显示降级，仍执行证据门控 | 召回可能下降 |
| 重排不可用 | 回退 RRF，标明失败 | 不保证重排质量 |
| 模型未配置、超时或空响应 | 提示服务不可用，不冒充正常生成 | 各组件超时不是端到端服务承诺 |
| 生成增删原则性限定语 | 限定事实范围内比较“原则上”出现次数，不一致时明确标记并展示原文要点，调试字段为 source_facts | 保守词面检查，可能将合并表达降级，不是完整语义校验；不计入正常生成通过数 |
| 无依据或对象不匹配 | 拒答，不让模型补业务规则 | 启发式门控，不是完整事实蕴含验证 |
| 合同解析或权限配置损坏 | 不生成成功审阅；关闭合同访问，制度问答继续 | 需要人工恢复配置或替换资料 |
| 并发反馈、写入失败 | 单进程共享锁、临时文件替换，保护原记录 | 多进程写入需事务存储 |
| 索引失效 | 校验指纹、数量、维度及数值，必要时重建 | 无跨文件的完整发布事务 |
| 资讯源失败 | 显示连接提示，不进入内部问答 | 辅助页面不作为核心交付条件 |

## 8. 评估与验收

| 层级 | 方法 | 解释边界 |
|---|---|---|
| 代码回归 | 空值、并发、解析、索引损坏、合同范围、接口及 UI 状态；隔离临时目录 | 不等于用户效果 |
| 固定检索 | 15 条案例的 Hit@k、MRR、门控拒答及引用覆盖代理指标 | 不是答案正确率 |
| 确定性回答回归 | 事实完整、排除无关内容、适配开关对照 | 不保证模型稳定或跨企业泛化 |
| 真实模型人工审计 | 10 条改写题逐项核对事实、限定语、来源及拒答，保留失败过程 | 修复后的题不是独立留出集 |
| 合同任务 | 多种写法、空白模板、原文位置、人工状态及导出版本 | 不等于完整风险识别率 |
| 企业试点 | 实际任务、人工金标准、对照耗时、反馈覆盖、运行和维护成本 | 不代表已经规模化 |

本地发布门槛：全量回归通过；高风险错答有回归；逐项验收关键事实、适用对象、条件、来源和访问范围。不能用其他通过用例抵消已知严重错误，不能降低预期答案换取通过。

真实试点门槛：授权、有效版本、真实身份、检索前权限和审计先通过；确认独立验收集及指标分母。越权、事实捏造、错误执行或关键遗漏未关闭时，不扩大使用范围。

共享服务门槛：至少两次独立交付证明流程和权限共性；双租户交叉泄漏为零；迁移可校验回滚；共享方式确有收益。无证据时继续独立部署或停止。

最新结果见[人工审计](docs/MANUAL_QA_AUDIT.md)与[发布检查](docs/RELEASE_REVIEW.md)。旧结果保留日期，不当作当前版本结论。
